@using MacsWebAssembly.Models
@inject IConfiguration config
@page "/activities"

@code {
    private DateTime _current = DateTime.Today;
    private Activities[]
    public Activities() {
        
    }
    
    
    
}